// consistent with Post model in backend
export const VISIBILITY = Object.freeze({
  PRIVATE: 0,
  PUBLIC: 1,
});

// consistent with Post model in backend
export const POST_TYPE = Object.freeze({
  NOTE: 0,
  QUESTION: 1,
});
